import 'package:flutter/material.dart';

class FirstAidTrainingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('First Aid Training'),
      ),
      body: Center(
        child: Text('First Aid Training Screen'),
      ),
    );
  }
}
