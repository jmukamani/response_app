import 'package:flutter/material.dart';

class LocationSharingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Location Sharing'),
      ),
      body: Center(
        child: Text('Location Sharing Screen'),
      ),
    );
  }
}
