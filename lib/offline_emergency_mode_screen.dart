import 'package:flutter/material.dart';

class OfflineEmergencyModeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Offline Emergency Mode'),
      ),
      body: Center(
        child: Text('Offline Emergency Mode Screen'),
      ),
    );
  }
}
